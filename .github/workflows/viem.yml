name: Viem Compatibility Tests

on:
  push:
    branches: [main, develop]
    paths:
      - 'tests/evm-tools-compatibility/viem/**'
      - 'scripts/setup-compatibility-tests.sh'
      - 'scripts/ci-viem.sh'
      - 'local_node.sh'
      - '.github/workflows/viem.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'tests/evm-tools-compatibility/viem/**'
      - 'scripts/setup-compatibility-tests.sh'
      - 'scripts/ci-viem.sh'
      - 'local_node.sh'
      - '.github/workflows/viem.yml'

permissions:
  contents: read

jobs:
  viem-compatibility:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive
      
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential curl git jq
      
      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-
      
      - name: Cache Node.js modules
        uses: actions/cache@v4
        with:
          path: tests/evm-tools-compatibility/viem/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('tests/evm-tools-compatibility/viem/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
      
      - name: Build and install evmd
        run: |
          make install
      
      - name: Run viem compatibility tests
        run: |
          chmod +x scripts/ci-viem.sh
          ./scripts/ci-viem.sh --verbose
        env:
          COMPAT_DIR: ${{ github.workspace }}/tests/evm-tools-compatibility
